{
  "provider_id": "azure_openai",
  "display_name": "Azure OpenAI",
  "version": "0.1.0",
  "capabilities": ["llm.chat_json", "llm.chat_text", "embedding.text"],
  "config_schema": {
    "type": "object",
    "required": ["endpoint", "api_version"],
    "properties": {
      "endpoint": {"type": "string"},
      "api_version": {"type": "string"}
    }
  },
  "credential_schema": {
    "type": "object",
    "required": ["api_key"],
    "properties": {
      "api_key": {"type": "string", "secret": true}
    }
  },
  "models": [],
  "entrypoint": "app.providers.azure_openai.provider:AzureOpenAIProvider",
  "implemented": false
}

