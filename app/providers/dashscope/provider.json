{
  "provider_id": "dashscope",
  "display_name": "DashScope",
  "version": "0.1.0",
  "capabilities": ["llm.chat_json", "llm.chat_text", "embedding.text", "stt.batch", "tts.synthesize"],
  "config_schema": {
    "type": "object",
    "properties": {
      "base_url": {"type": "string"}
    }
  },
  "credential_schema": {
    "type": "object",
    "required": ["api_key"],
    "properties": {
      "api_key": {"type": "string", "secret": true}
    }
  },
  "models": [],
  "entrypoint": "app.providers.dashscope.provider:DashScopeProvider",
  "implemented": false
}

