{
  "provider_id": "tencent_cloud_speech",
  "display_name": "Tencent Cloud Speech",
  "version": "0.1.0",
  "capabilities": ["stt.streaming", "stt.batch", "tts.synthesize"],
  "config_schema": {
    "type": "object",
    "required": ["region"],
    "properties": {
      "region": {"type": "string"}
    }
  },
  "credential_schema": {
    "type": "object",
    "required": ["secret_id", "secret_key"],
    "properties": {
      "secret_id": {"type": "string", "secret": true},
      "secret_key": {"type": "string", "secret": true}
    }
  },
  "models": [],
  "entrypoint": "app.providers.tencent_cloud_speech.provider:TencentCloudSpeechProvider",
  "implemented": false
}

