# 054 · 消除巡检引入的语音停顿

状态：verified（本机负载、浏览器及完整回归）；不代表生产或真人整场验收。

## 证据与修复

053后的真实试音，TTS供应读取累计仅43/14.5ms，但共享事件循环最大延迟333/316ms，超过240ms缓冲。对当前40场会话严格只读重演旧巡检（包含逐场读取）测得中位297.192ms、最大354.550ms；不是把文本按句拆开合成引入的固定间隔。单场读取约1.4ms，历史全量读取及重复检查才是此轮修复对象。未凭这些数值推断原场次浏览器实际欠载次数。

- `InterviewSessionRepository.watchdog_candidates`通过组织与状态筛选，SQLite/PG部分索引使历史完成会话不进入音频线程的反序列化。Memory保持相同候选语义。
- 无任务command claim、普通会话get及无过期接管查询只读；有实际任务后仍进入原写事务重新核验owner、lease、epoch、控制代次、截止时间及版本。
- 截止巡检只读发现到期候选后，按场写事务再验证状态、输入完成标记和三种截止日期来源。发现后续约或完成不会误关闭；接管续约不会因旧候选而被撤销。
- 保留240ms缓冲及全部播放/采集围栏，不通过增大缓冲、删除历史或放宽drained来掩盖中断。无新增业务API、状态字段、Prompt或用户配置。

## 验收与边界

新增持有SQLite写锁、空轮询、候选后状态变化、历史隔离/索引、失效owner和并发claim回归；全量前后端回归覆盖打断、字幕、持续收音、暂停与恢复。真实LiveKit随机房间只播合成PCM或公开中文，后台负载只用进程内副本和只读成本，不领取真实命令、不发候选资料。

浏览器质量以worklet渲染时间、期望/接收/消费样本和欠载计数为准，墙钟回调仅辅助。该探针不包含真实候选人麦克风、上行STT或全部API端点，因此不能等同真人全双工整场验收。

## 运行与恢复

部署前须确认活动候选人及worker工作为空，正常停止并复用原环境重启。SQLite启动幂等建立索引；PG007仅提供迁移文件，目标数据库另验。不恢复真实面试或修改历史内容。必要时只回退054增量，保留053及之前改动；索引不改业务行，可保留。

## 实际结果

- 16秒合成LiveKit音频＋20ms空poll/终态读取＋1秒接管巡检＋完整只读表达围栏：旧全量发包最大310.703ms、完成16.933秒；新定向发包最大49.244ms、完成16.011秒，384000样本全部一致。新巡检p50 0.079ms/max 0.187ms，循环最大延迟28.664ms。只读扫描副本不包含真实写锁和全部API负载。
- 正式压缩播放器在浏览器运行相同读取负载：20秒合成PCM和118字公开中文（21.52秒）均0欠载/0额外静音；worklet渲染跨度恰好等于内容时长，接收/渲染/预期样本分别480000和516480，全部相同。中文运行循环最大延迟27.467ms。
- 故障注入3秒停供：48000样本全部恢复，渲染跨度5000ms（内容2000ms），72000静音样本恰与断供相符；不冒充无中断。首声后0.2ms进入清理，未发送drained；服务端探针中断标记是本次主动取消场景结果，不是业务面试报错。
- 全后端2307 passed/8 skipped（159.16s）、前端384 passed/24 files；构建、隔离pycache编译、文档链接和diff检查通过。外部环境skip不计作生产验证。独立审查未发现本轮新增阻断问题。
- 空闲检查后正常重启本机API80893、worker80894（含Beat），healthz/readyz均200且ready，worker单节点pong及派发正常。前端源码业务部分未变，重建7份产物与已服务文件hash完全一致，未多做静态切换。
- SQLite实际查询命中两个新增部分索引；PG007未在真实目标库执行。3个媒体容器running/OOMfalse，维护窗口API/worker锁错误、音频背压、Traceback、未读取Task异常均0；原会话整体JSON摘要与维护前相同，未恢复或重写。4个探针随机房间及两个临时服务已清理。

证据保留于 `/private/tmp/interviewer-054-{combined-watchdog-results,browser-acceptance,runtime-summary}.json`，以及同前缀backend/frontend/build/restart/verify日志。目录为本机临时诊断，不含候选人文本或凭据。首次探针构建缺alias、核验脚本缺模块路径等工具失误已更正，失败日志保留；详见操作日志。
